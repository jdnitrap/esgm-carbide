# Prototypes parked in ESGR

`graph_prototype.py` is the two-level domain-routing graph CLI
(Math / Physics / English A→B chains).

Source is stored packed so the text commit path does not mangle it.
Materialize with:

```bash
python3 prototypes/unpack.py
python3 prototypes/graph_prototype.py
```

`Learning-Hub.md` was not added — that is a Think-vault index, not ESGR code.
